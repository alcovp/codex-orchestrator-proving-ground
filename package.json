{
  "name": "main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build",
    "check": "tsc --noEmit",
    "dev": "vite",
    "preview": "vite preview",
    "deploy": "sh -c 'git add -A && if git diff --cached --quiet; then echo \"No changes to commit\"; else git commit -m \"chore: deploy\" && git push origin HEAD; fi'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/three": "^0.182.0",
    "typescript": "^5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "three": "^0.182.0"
  }
}
